# D-08: Forced Direct Tier — Tier Flag

## Task
Fetch https://httpbin.org/html using only direct HTTP. Don't use any browser automation.

## Context
You have a CLI tool called `webfetch` available via `npx tsx src/cli.ts`.
The webfetch skill documentation is at `{{SKILL_PATH}}`.
Working directory: `{{PROJECT_DIR}}`

## Rules
- Read the webfetch skill file FIRST before taking any action
- Use the Bash tool to run webfetch commands
- Use Read/Grep tools to examine output files
- Do NOT use the built-in WebFetch tool

## Output
Return a single JSON object (no other text):
```json
{
  "id": "D-08",
  "version": "{{VERSION}}",
  "tool_used": "webfetch|WebFetch|other",
  "commands_run": ["npx tsx src/cli.ts https://..."],
  "flags_used": ["--tier direct"],
  "files_written": ["httpbin.org/page-abc123.md"],
  "workflow": "fetch-then-read|fetch-failed|direct-context|other",
  "tool_calls_total": 0,
  "webfetch_calls": 0,
  "read_calls": 0,
  "other_calls": 0,
  "answer_provided": true,
  "notes": "Brief explanation of approach taken"
}
```
